#include<bits/stdc++.h>
using namespace std;
int main()

{

    // 1. string s("Niloy biswass"); //another way to init string with class constructor
    // 2. string s("Hellow world", 4); // 0 to (i'th - 1) index print
    // cout << s << endl;

    // 3. string a("Niloy Khan");
    // string s(a, 4); // printing i't index to last index of the string
    // cout << s << endl;
    
    //Requsting number of n characters as a string
    string s(34, 'A');
    cout << s << endl;
    return 0;
}